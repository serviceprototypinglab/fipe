"""
Bewerten Sie die folgenden Aussagen jeweils mit einer Zuweisung
eines gültigen booleschen Wertes: True für richtig und
False für falsch.

(10 Punkte - je 1 Punkt pro Bewertung)
"""

# Aussage X:
# Die Methode __init__(self) wird in der objektorientierten
# Programmierung als 'Konstruktor' bezeichnet.

bewertungX = True

# Aussage X:
# Dank der Einführung von 5G werden Internetverbindungen so schnell,
# dass bei der Verwendung des Moduls 'urllib' künftig keine Exceptions
# mehr auftreten.

bewertungX = False

# Aussage X:
# Das Modul 'profile' kann genutzt werden, um Flaschenhälse in der
# Programmausführung herauszufinden. Diese können dann gezielt
# optimiert werden.

bewertungX = True

# Aussage X:
# Der Zugriff auf Attribute eines Objekts von ausserhalb der zugehörigen
# Klassendefinition (und abgeleiteten Klassen) kann unterbunden werden,
# indem der Attributname mit 'private' beginnt.

bewertungX = False

# Aussage X:
# Funktionen (oder Methoden) können als Parameter an andere Funktionen
# (oder Methoden) übergeben werden.

bewertungX = True

# Aussage X:
# Mit systematisch erstellten Unittests lässt sich die Wahrscheinlichkeit
# unbemerkter Programmierfehler reduzieren und somit die Qualität eines
# Python-Projekts steigern.

bewertungX = True

# Aussage X:
# Gibt man eine Instanz mit print aus, wird die Methode __str__() aufgerufen.

bewertungX = True

# Aussage X:
# Sehr häufige Aufrufe einer Funktion A aus einer Funktion B können zu
# Performanzeinbussen führen. Abhilfe schafft eine Refaktorisierung, indem
# der Code von A innerhalb von B direkt eingefügt wird.

bewertungX = True

# Aussage X:
# Python erlaubt Polymorphismus, das heisst die Ableitung einer Klasse
# von mehr als einer Elternklasse.

bewertungX = True

# Aussage X:
# Eine Guard-Zeile 'if __name__ == "__main__":' dient der Unterscheidung,
# ob eine Python-Datei als Skript ausgeführt oder als Modul eingebunden
# wird.

bewertungX = True

# Aussage X:
# Erbt eine Klasse A von Klasse B, sollte der Konstruktor von B im Konstruktor von A aufgerufen werden.

bewertungX = True

# Aussage X:
# Erbt eine Klasse A von Klasse B, kann eine Methode aus B definitiv nicht mehr aufgerufen werden,
# weil sie stets von A überschrieben wird.

bewertungX = False

# Aussage X:
# Zu einer Klasse können mehrere Instanzen erstellt werden.

bewertungX = True

# Aussage X:
# Ein Attribut kann in der Sichtbarkeit ausserhalb der zugehörigen Klasse eingeschränkt werden.

bewertungX = True

# Aussage X:
# Eine Methode hat immer eine Rückgabe.

bewertungX = False

# Aussage X:
# In der Python-Syntax ist der erste Parameter einer Methode immer self.

bewertungX = True

# Aussage X:
# Ein Attribut der Vaterklasse kann in einer Kindklasse nie verwendet werden.

bewertungX = False

# Aussage X:
# Eine Klasse muss immer einen explizit dafür programmierten Konstruktor haben.

bewertungX = False

# Aussage X:
# Eine Klasse muss immer zuerst instanziiert werden, bevor man auf deren Methode oder Attribute zugreifen kann.
# Einzige Ausnahme sind statische Methoden.

bewertungX = True

# Aussage X:
# Mit dem Schlüsselwort lambda kann in Python der Abstand zwischen zwei Punkten berechnet werden.

bewertungX = False
